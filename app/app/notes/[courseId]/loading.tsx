import { Card, CardBody } from "@/components/ui/card";

export default function NotesPageLoading() {
  return (
    <Card>
      <CardBody className="p-8 text-sm text-muted">Loading notes...</CardBody>
    </Card>
  );
}
