import { SectionLinkIndex } from "@/features/professor/pages/section-link-index";

export default function SectionGradebookIndexPage() {
  return (
    <SectionLinkIndex
      title="Gradebook"
      subtitle="Review submission status and scores across your sections."
      variant="gradebook"
    />
  );
}
