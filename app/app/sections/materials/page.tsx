import { SectionLinkIndex } from "@/features/professor/pages/section-link-index";

export default function SectionMaterialsIndexPage() {
  return (
    <SectionLinkIndex
      title="Materials"
      subtitle="Post notes, references, and handouts for each class section."
      variant="materials"
    />
  );
}
