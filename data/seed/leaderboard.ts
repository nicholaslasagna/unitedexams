import type { LeaderboardEntry } from "@/lib/types";

// Intentionally empty: leaderboard should only show real users.
// Keep this export for future backend hydration compatibility.
export const seededLeaderboard: LeaderboardEntry[] = [];
