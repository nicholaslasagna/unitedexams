export const THEME_DEFAULTS = {
  mode: "dark" as const,
  accentHue: 265,
  accentStrength: 60,
  palette: "violet" as string
};

export const ACCENT_RANGE = {
  minHue: 0,
  maxHue: 360,
  minStrength: 0,
  maxStrength: 100
};
