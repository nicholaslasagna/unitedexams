export const THEME_DEFAULTS = {
  mode: "dark" as const,
  accentHue: 38,
  accentSaturation: 92,
  accentLightness: 50,
  accentStrength: 56,
  palette: "amber" as string
};

export const ACCENT_RANGE = {
  minHue: 0,
  maxHue: 360,
  minStrength: 0,
  maxStrength: 100
};
